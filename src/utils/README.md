# Utils

Pure constants and deterministic helper functions belong here.

Use this folder for code that turns inputs into outputs without side effects.

`notificationTap` validates the small, non-clinical payload used to point a
notification tap toward current server data.
