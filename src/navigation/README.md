# Navigation

Navigation-specific configuration belongs here when it is needed.

NurseFlow currently uses Expo Router, so route registration stays in `src/app/`
and the screen UI lives in `src/screens/`.
